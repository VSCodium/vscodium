{
  "_comment": "Per-project endpoint manifest. Lint cross-checks every kind: action page's 'endpoint' field against this list. Maintain manually OR generate from your app's OpenAPI / MCP introspection. Empty list disables endpoint validation.",
  "endpoints": []
}
