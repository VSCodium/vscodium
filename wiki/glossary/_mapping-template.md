---
slug: shadow-ide/glossary/TERM_SLUG
project_slug: shadow-ide
kind: glossary
audience: [user, agent, dev]
version: 1
last_updated:
mapped_date:
status: stub
tags: []
---

<!--
STRUCTURAL CONTRACT — READ BEFORE EDITING

A glossary entry defines a CROSS-CUTTING DOMAIN TERM that appears in multiple
places in the wiki. Glossary is NOT a dumping ground for concepts that don't
fit elsewhere — it's specifically for terms a new dev or user needs to
understand to read other pages.

Scope guidance — DO include:
- Domain terms specific to this app's industry (e.g. "Enrollee", "Live Photo",
  "Sobriety Test", "BAC", "Probationer", "Court Order").
- Acronyms and codes used internally (e.g. "OLI app", "IID", "EM").
- App-specific UX terms with non-obvious meaning (e.g. "Login Reason").

Scope guidance — do NOT include:
- General programming terms (REST, JSON, OAuth) — link to external docs.
- Things that fit cleanly into a feature/component/integration page — put
  them there instead.
- Terms used only in one other page — define them inline on that page.

Lint flags glossary entries with no inbound wikilinks from other wiki pages
(orphan check). If a term is genuinely cross-cutting, at least 2 other pages
should reference it.

When creating a glossary entry:

1. Read frontmatter-schema.md.
2. Copy this file to OUTPUT_PATH/glossary/[term-slug].md.
3. Replace shadow-ide and TERM_SLUG (kebab-case of the term).
4. Audience: default [user, agent, dev] — glossary is for everyone.
5. Cross-link from at least 2 other wiki pages.

Section order: Term → Definition → Used in → Related terms → Notes
-->

# Term

> One-line definition. Plain language. End-user-readable.

## Term

<!-- The canonical term (in singular form). If there are common variants or
     abbreviations, list them under "Also known as":

     **Live Photo**
     Also known as: live capture, liveness photo, biometric capture. -->

## Definition

<!-- A 2–4 sentence definition. Avoid circular definitions (don't define a
     term using the term itself). Avoid jargon — if you must use other
     domain terms, link to their glossary entries.

     Example:
     "A short photo capture taken at login that proves the user is physically
     present and alive (i.e., not a still photo or video being shown to the
     camera). Used in OLI apps to satisfy compliance requirements that the
     monitored individual personally completed each login." -->

## Used in

<!-- Bulleted list of wiki pages where this term appears with significance.
     Format:
     - [[features/enrollee-login]] — required at first-time login.
     - [[workflows/first-time-login]] — step 5 of the workflow.

     Lint flags glossary entries with fewer than 2 entries here as orphans. -->

## Related terms

<!-- Bulleted list of [[glossary/other-term]] entries that connect to this one.
     Helps users navigate related concepts. -->

## Notes

<!-- Optional. History, why the term was coined, links to external sources,
     or notes for the agentic assistant about how to explain this term to
     end users in different contexts. -->
